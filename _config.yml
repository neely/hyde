#
# This file contains configuration flags to customize your site
#

# Name of your site (displayed in the header)
name: Your Blog Name

# Name of the author
author: Your Name

# Short bio or description (displayed in the header)
description: A short description of what this blog is about.

# URL of your avatar or profile pic (you could use your GitHub profile pic)
avatar: /images/avatar-placeholder.png

#
# Flags below are optional
#

# Includes an icon in the footer for each username you enter
footer-links:
  dribbble:
  email: you@example.com
  facebook:
  flickr:
  github: YOUR-GITHUB-USERNAME
  instagram:
  linkedin:
  pinterest:
  rss: rss
  twitter:
  stackoverflow:
  youtube: # channel/<your_long_string> or user/<user-name>
  googleplus:
  playconsole:
  mastodon: # <your-mastodon-instance>/@<your-user-name> eg. fosstodon.org/@username
  bluesky: # bsky.app/profile/<your-handle>

# Enter your Google Analytics web tracking code (e.g. UA-2110908-2) to activate tracking
# google_analytics:

# For newer "GA4" analytics, use the following instead of the "UA" entry above
# google_analytics_ga4: G-XXXXXXXXXX

# Your website URL (e.g. http://yourusername.github.io or http://www.yourdomain.com)
# Used for Sitemap.xml and your RSS feed
url: https://YOUR-GITHUB-USERNAME.github.io
enforce_ssl: https://YOUR-GITHUB-USERNAME.github.io

# If you're hosting your site at a Project repository on GitHub pages
# (http://yourusername.github.io/repository-name)
# and NOT your User repository (http://yourusername.github.io)
# then add in the baseurl here, like this: "/repository-name"
baseurl:

#
# !! You don't need to change any of the configuration flags below !!
#

# Hyde / Reverie use clean title-based URLs with no date in the path.
# A post named _posts/2026-06-17-My-Title.md becomes yoursite.com/My-Title/
# The date prefix on the filename only controls sort order in _posts/ — it
# never appears in the live URL. This is what the Hyde composer expects.
permalink: /:title/

# The release of Jekyll Now that you're using
version: v1.2.0

# Jekyll 3 now only supports Kramdown for Markdown
kramdown:
  # Use GitHub flavored markdown, including triple backtick fenced code blocks
  input: GFM
  # Jekyll 3 and GitHub Pages now only support rouge for syntax highlighting
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    # Use existing pygments syntax highlighting css
    css_class: 'highlight'

# Set the Sass partials directory, as we're using @imports
sass:
  style: :expanded # You might prefer to minify using :compressed

# Use the following plug-ins
plugins:
  - jekyll-sitemap # Create a sitemap using the official Jekyll sitemap gem
  - jekyll-feed # Create an Atom feed using the official Jekyll feed gem
  - jekyll-seo-tag
  - jekyll-paginate

include: ['_pages']

paginate: 6
paginate_path: /page:num/

# Exclude these files from your production _site
# compose.html is intentionally NOT excluded — it needs to be served as a
# static page at yoursite.com/compose.html so you can use it to write posts.
exclude:
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - CNAME
  - HYDE-SETUP.md
